<textarea name="form[{{ name }}]" id="form_{{ name }}" class="richtext-widget" {% if parameters['required'] is defined and parameters['required'] %}required="required"{% endif %}>{{ value | raw }}}</textarea>
